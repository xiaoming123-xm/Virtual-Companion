# 国际化脚本指南

## 概述

本文档说明项目中用于检查和清理翻译键的脚本用法。

- `npm run check-i18n` - 检查翻译键的使用情况
- `npm run clean-i18n` - 删除未使用的翻译键

## 检查翻译键

```bash
npm run check-i18n
```

### 功能

1. **检查未定义的翻译键** - 代码中使用但翻译文件中不存在的键
2. **检查未使用的翻译键** - 翻译文件中定义但代码中未使用的键
3. **检查中英文一致性** - 确保 zh.json 和 en.json 的键完全一致
4. **检查错误用法** - 检测 `t('key') || '默认值'` 这种错误模式
5. **检查硬编码 UI 文案** - 发现代码中的硬编码中文或英文界面文本

### 输出示例

```
🔍 开始检测国际化翻译键...

📚 加载翻译文件...
  ✓ 中文翻译键: 286 个
  ✓ 英文翻译键: 286 个

🔄 检查中英文翻译键一致性...
  ✓ 中英文翻译键完全一致

📂 扫描代码文件...
  ✓ 找到 70 个文件

🔎 提取使用的翻译键...
  ✓ 代码中使用了 286 个翻译键

❌ 检查未定义的翻译键...
  ✓ 所有翻译键都已定义

🗑️  检查未使用的翻译键...
  ✓ 所有翻译键都在使用中

⚠️  检查错误用法 t('key') || '默认值'...
  ✓ 未发现错误用法

🈲 检查硬编码 UI 文案...
  ✓ 未发现硬编码 UI 文案

📊 检测总结:
──────────────────────────────────────────────────
✅ 所有检查通过！国际化配置完美！
──────────────────────────────────────────────────
```

## 清理未使用的翻译键

```bash
npm run clean-i18n
```

### 功能

自动删除 `zh.json` 和 `en.json` 中所有未使用的翻译键。
删除前会先重新扫描整个前端代码目录，避免因为扫描范围不完整而误删。

### 使用场景

- 重构代码后清理废弃的翻译键
- 定期维护翻译文件，保持代码整洁
- 删除实验性功能的翻译键

### 注意事项

⚠️ **使用前请确保：**

1. 代码已提交到 Git，以便在需要时回滚
2. 确认列出的"未使用"键确实不再需要
3. 某些键可能在动态场景中使用（如配置文件），脚本无法检测到

### 输出示例

```
🗑️  检查未使用的翻译键...
  ℹ️  未使用的翻译键 (18 个):
    - chat.selectModel
    - chat.settings.temperature
    - ...

🧹 开始删除未使用的翻译键...
  ✅ 成功删除 18 个未使用的翻译键
  📝 已更新文件:
    - frontend/locales/zh.json
    - frontend/locales/en.json
```

## 工作原理

### 扫描范围

脚本会扫描 `frontend/` 下所有 `.ts`, `.tsx`, `.js`, `.jsx` 文件，排除以下目录：

- `node_modules/`
- `dist/`
- `.git/`
- `locales/`
- `scripts/`

这意味着现在会覆盖：

- `components/`
- `pages/`
- `hooks/`
- `store/`
- `contexts/`
- `utils/`
- 根目录文件（如 `App.tsx`）

### 检测规则

1. **翻译键提取**
   - 匹配所有 `t('key')` 或 `t("key")` 调用
   - 支持模板字符串中的调用：`` `${t('key')} ${name}` ``
   - 支持带参数的调用：`t('key', { name: 'value' })`

2. **硬编码 UI 文案检测**
   - 检测 JSX 裸文本
   - 检测 `placeholder`、`title`、`aria-label`、`confirmText` 等 UI 属性
   - 同时检测中文和英文界面文案
   - 排除注释行和指定忽略文件（如样式演示页）

3. **显式翻译键声明检测**
   - 除了 `t('key')` 外，也会提取类似 `translationKey: 'admin.xxx'` 的映射声明
   - 适用于公共能力映射、配置表等非直接调用 `t()` 的场景

4. **错误用法检测**
   - 检测 `t('key') || '默认值'` 模式
   - 这种用法会导致翻译键检测失败

## 最佳实践

### 开发流程

1. **添加新功能前**
   ```bash
   npm run check-i18n
   ```
   确保当前翻译状态良好

2. **开发过程中**
   - 先在 `locales/*.json` 中添加翻译键
   - 然后在代码中使用 `t('key')`
   - 不要使用 `t('key') || '默认值'`

3. **提交代码前**
   ```bash
   npm run check-i18n
   ```
   确保没有未定义的翻译键

4. **定期维护**
   ```bash
   npm run clean-i18n
   ```
   清理未使用的翻译键

### 翻译键命名规范

- 使用小驼峰命名：`newChat`、`selectModel`
- 按功能模块分组：`sidebar.*`、`admin.*`、`chat.*`
- 保持键名简洁且语义清晰

### 常见前缀

- `sidebar.*` - 侧边栏相关
- `admin.*` - 管理后台相关
- `chat.*` - 聊天界面相关
- `settings.*` - 设置相关
- `app.*` - 应用全局相关

## 故障排除

### 脚本报告"未使用"但实际在使用

可能原因：
1. 使用了动态键：`t(dynamicKey)` - 脚本无法静态分析
2. 键在配置文件或数据库中使用
3. 键在注释或文档中

解决方案：
- 避免使用动态键
- 如果必须使用动态键，在代码中添加注释说明
- 手动保留这些键，不要运行 `clean-i18n`

### 脚本未检测到某些文件

检查：
1. 文件扩展名是否为 `.ts`, `.tsx`, `.js`, `.jsx`
2. 文件是否在 `frontend/` 目录下且不在排除目录中
3. 文件是否在排除目录中（`node_modules`, `dist`, `.git`）

### 删除后发现需要恢复

使用 Git 恢复：
```bash
git checkout -- frontend/locales/zh.json frontend/locales/en.json
```

## 技术细节

### 正则表达式

```typescript
// 匹配 t() 调用（包括模板字符串中）
const tCallRegex = /\bt\s*\(\s*['"]([^'"]+)['"]\s*[,)}\]]/g;
```

### 删除算法

递归遍历翻译对象，删除指定的键，同时清理空的父对象。

### 文件格式

保持 JSON 文件的格式化（4 空格缩进）和末尾换行符。
